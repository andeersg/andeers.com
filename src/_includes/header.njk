<header class="l-grid l-pad-small">
    <div class="header l-grid-wide">
      <h1 class="site-name"><a href="/">Andeers.com</a></h1>
      <nav class="primary-nav">
        {% for item in menu.items %}
          <a class="primary-nav__item{{ ' primary-nav__item--current' if page.url == item.link }}" href="{{ item.link }}">{{ item.text }}</a>
        {% endfor %}
      </nav>
    </div>
  </header>